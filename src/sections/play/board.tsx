// board.tsx is no longer used — play page now uses MultiplayerGame component.
// Kept as an empty stub to avoid import errors from any stale references.
export default function BoardContainer() {
  return null;
}
